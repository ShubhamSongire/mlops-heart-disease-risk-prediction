# Video Instructions

Record a short video demonstrating:
- Clone repo and install requirements
- Download data and run training (fast)
- Show MLflow UI with runs
- Build Docker image and run container
- Call `/predict` and `/metrics` endpoints
- Apply Kubernetes manifests and access service
- Briefly show GitHub Actions CI results

Tips:
- Use PowerShell on Windows
- Keep it under 5 minutes
- Zoom in on terminal and browser
