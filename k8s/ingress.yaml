apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: heart-api-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: heart-api-service
                port:
                  number: 8000
